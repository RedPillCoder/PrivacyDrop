"""PrivacyDrop — single source of truth for the version number.

Imported by app.py at runtime, read by the build/release scripts so the
version string is never duplicated.  Bump it here before a release.
"""

APP_VERSION = "1.1.0"
