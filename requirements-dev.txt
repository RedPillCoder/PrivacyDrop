# Development & test dependencies
#
# Install with:  pip install -r requirements-dev.txt
# (Also installs the runtime deps via -r requirements.txt)

-r requirements.txt

# Test fixtures — used only by tests/ to create realistic sample documents.
# The app itself never imports these; sanitisation uses the stdlib zipfile.
python-docx>=1.0.0
openpyxl>=3.1.0
python-pptx>=1.0.0
